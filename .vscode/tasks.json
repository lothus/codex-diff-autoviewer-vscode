{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Codex Auto Open",
      "type": "shell",
      "command": "npm run compile",
      "options": { "cwd": "${workspaceFolder}/vscode-extension" },
      "problemMatcher": "$tsc",
      "group": "build"
    }
  ]
}
